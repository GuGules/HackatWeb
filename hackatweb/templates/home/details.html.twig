{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="row row-cols-md-12">
    <div class="cols mb-4">
        <div class="card-body">
            <p>{{leHackathon.nom}}</p>
            <p>{{leHackathon.date_debut|date("d/m/Y H:i")}}</p>
            <p>{{leHackathon.date_fin|date("d/m/Y H:i")}}</p>
            <p>{{leHackathon.date_limiteReg|date("d/m/Y H:i")}}</p>
            <p>{{leHackathon.nb_places}}</p>
        </div>
    </div>
</div>
{% endblock %}
